/*
 *  loadspec: @file
 *
 *  Answers the loader descriptor for the reader package.
 */
"use strict";

module.exports = {
    modules: {
        ReadPoint,
    },
    path: __dirname
};

var ReadPoint;